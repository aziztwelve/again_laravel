# Задача: Актуализация статусов и логики карточки заказа

**Статус:** ✅ Реализовано полностью. Все пункты плана-результата и все
открытые вопросы закрыты.
**Дата создания:** 2026-08-19
**Дата последнего обновления:** 2026-08-21 (исправлена ручная отмена из карточки)
**Раздел админки:** Заказы → Все заказы → карточка заказа

---

## Чек-лист реализации

| № | Пункт плана | Статус | Комментарий |
|---|---|:---:|---|
| 1 | Актуализировать список статусов (`OrderStatus`) | ✅ Сделано | `SHIPPED_EXPORT` удалён (в БД не было заказов с этим статусом — 0 записей), `RETURN_PAYMENT`/«Возврат оплаты» добавлен в конец. Синхронизировано на фронте (`again_dashboard/HomeChart.vue`), остальной UI подтягивает список динамически через `StatusService::getOrderStatuses()` |
| 2 | Описание логики по каждому статусу | ✅ Сделано | «Отгружен»/«Доставлен»/«Возврат товара» теперь переключаются автоматически по статусу доставки ЯД/СДЭК (Observer'ы, см. ниже) |
| 3 | Синхронизация с МойСклад | ✅ Сделано, но **по другой модели**, чем в исходном плане | Решение изменено в процессе (см. «Важное изменение решения» ниже): вместо резерва — сразу физическое списание через `demand` при создании заказа |
| 4 | Крон-автоотмена неоплаченных за 2 часа | ✅ Сделано | `orders:cancel-unpaid`, каждые 5 мин, проверяет `new` и `processing`, см. ниже |
| 5 | Кнопка «Отменить оплату» + эндпоинт refund | ✅ Сделано | Открытый вопрос №1 решён: два разных эндпоинта под разную авторизацию (см. ниже). Кнопка в `again_dashboard` (`OrderStatuses.vue`), видна только для оплаченных заказов |

---

## Важное изменение решения (после обсуждения)

Исходный план (резервирование `reserve` на позициях `customerorder`) был
**заменён** на более простую и надёжную схему после того, как тесты показали
проблему: резерв не защищал от перепродажи на сайте (сайт проверяет остаток
по локальному `stock_quantity`, а не по резерву в МойСклад), и переход в
статус «Отгружен» сам сбрасывал резерв без физического списания.

**Принято решение:** товар списывается **сразу при создании заказа**
(независимо от оплаты) через документ «Отгрузка» (`demand`), а не
резервируется. Если оплата не поступает за 2 часа — товар возвращается на
склад (демонд распроводится). Это можно грубо описать так: «Новый заказ
на сайте = списание на складе сразу», а не «резерв → списание при
отгрузке».

**Что это означает для нерешённых пунктов 1-2, 5:**
- Логика для статуса «Отгружен» из исходного плана («создать `demand` при
  переходе в Отгружен») **больше не актуальна** — списание происходит
  раньше, при создании. Статус «Отгружен» теперь просьто отражает, что
  сервис доставки забрал заказ, без дополнительной складской операции.
- Статус «Возврат товара» по-прежнему **не реализован** — при возврате
  товара от клиента нужно решить (см. открытый вопрос №4), распроводить
  тот же `demand`, или создавать отдельный документ `salesreturn`.

---

## Реализовано (детали)

### 1. Список статусов

- `App\Enums\OrderStatus`: убран `SHIPPED_EXPORT`, добавлен `RETURN_PAYMENT`
  ('return_payment', label «Возврат оплаты», цвет `#9333ea`) в конец списка.
- Проверено перед удалением: `SELECT status, COUNT(*) FROM orders GROUP BY
  status` — заказов с `shipped_export` не было (0 записей), удаление
  безопасно, миграция данных не требовалась.
- `again_dashboard/src/components/dashboard/charts/HomeChart.vue`
  (`STATUS_META`) синхронизирован вручную (хардкодит статусы для графика).
  Остальной UI (выбор статуса в карточке заказа, `OrderStatuses.vue`)
  подтягивает список динамически через `GET` эндпоинт
  (`StatusService::getOrderStatuses()` → `OrderStatus::toArray()`), поэтому
  не требовал отдельных правок.

### 2. Автоматизация статусов «Отгружен»/«Доставлен»/«Возврат товара»

- **`App\Observers\YandexOrderObserver`** и **`App\Observers\CdekOrderObserver`**
  (новые) — слушают `updated()` на моделях `YandexOrder`/`CdekOrder`,
  реагируют на изменение поля `internal_status` (обновляется существующими
  `PollYandexDeliveryStatusesJob`/`PollCdekDeliveryStatusesJob`, которые
  уже работали каждые 10 минут — сами Job'ы не менялись).
- Оба Observer'а зарегистрированы в `AppServiceProvider::boot()`
  (`Model::observe()` — по тому же паттерну, что уже был для
  `Product`/`ProductVariant`).
- Маппинг статуса доставки → `OrderStatus`, переключение через
  существующий `OrderCreationService::updateOrderStatus()` (который уже
  ставит синхронизацию с МойСклад в очередь, если заказ там выгружен —
  переиспользован без изменений):

  | Yandex `internal_status` | СДЭК `internal_status` | → `OrderStatus` |
  |---|---|---|
  | `picked_up` | `IN_TRANSIT` | `SHIPPED` |
  | `delivered` | `DELIVERED` | `DELIVERED` |
  | `returning`, `returned` | `RETURNED` | `PRODUCT_RETURN` |

- Защита от откатывания финальных/ручных статусов: если заказ уже
  `CANCELLED` или `RETURN_PAYMENT`, автоматика доставки его не
  переписывает (проверено тестом — заказ, отменённый вручную, остался
  `cancelled` после прихода `returning` от Яндекса).
- `cancelled`/`failed` статусы доставки **не** обрабатываются в этих
  Observer'ах намеренно — отмена заказа управляется только через
  `cancelOrder()` (ручная или крон), чтобы не было двух независимых
  путей, которые могут разойтись в поведении (списание/возврат товара
  в МойСклад, история и т.д.).

**Подтверждено тестами на реальных данных** (создание `YandexOrder`/
`CdekOrder`, ручное изменение `internal_status`, проверка `Order::status`):
- Yandex `picked_up` → заказ `shipped` ✅
- Yandex `delivered` → заказ `delivered` ✅
- СДЭК `IN_TRANSIT` → заказ `shipped` ✅
- СДЭК `DELIVERED` → заказ `delivered` ✅
- Отменённый заказ (`cancelled`) не переписывается на `returning` от
  Яндекса ✅

### 3. Синхронизация с МойСклад — списание сразу при заказе

- **`App\Services\MoySklad\DemandService`** (новый сервис):
  - `shipOrder(Order $order): string` — создаёт и проводит документ
    «Отгрузка» (`demand`), привязанный к заказу через `customerOrder`.
    Идемпотентно (хранит `moysklad_demand_uuid` на заказе, повторный вызов
    не создаёт дубль).
  - `returnOrderStock(Order $order): void` — распроводит существующий
    `demand` (`applicable: false`), возвращая товар на склад без удаления
    документа. Идемпотентно (если демонда нет — не делает ничего).
- **`App\Jobs\SyncOrderToMoySkladJob`** — после `pushOrder()` (создание/
  обновление `customerorder`) вызывает `DemandService::shipOrder()`, если
  заказ не отменён.
- **`App\Jobs\ReturnOrderStockToMoySkladJob`** (новый Job) — асинхронно
  вызывает `returnOrderStock()`.
- **`OrderCreationService::cancelOrder()`** — при отмене (любой, в т.ч. от
  крона) ставит `ReturnOrderStockToMoySkladJob` в очередь, если у заказа
  есть `moysklad_demand_uuid`.
- **`OrderUpdateService::update()`** — при ручном переходе в `cancelled`
  через inline-редактор карточки также ставит `ReturnOrderStockToMoySkladJob`.
  Раньше этот UI использовал общий `PUT /orders/{id}`, обходил
  `OrderController::updateStatus()`/`cancelOrder()` и поэтому менял статус и
  документ заказа в МойСклад, но не распроводил `demand`.
- **Резерв убран** из `OrderService::buildPositions()` — позиции
  `customerorder` больше не передают поле `reserve`.
- Новая колонка `orders.moysklad_demand_uuid` (миграция
  `2026_08_19_074500_add_moysklad_demand_uuid_to_orders_table.php`).

**Подтверждено тестами на реальных данных (не эмуляция):**
- Создание заказа на 2 шт → физический остаток в МойСклад `100 → 98`.
- Отмена того же заказа → остаток `98 → 100`, лог:
  `MoySklad: товар возвращён на склад (demand распроведён)`.
- `check_stock()` (используется синхронизацией товаров) переключён на
  отчёт `/report/stock/all` (отдаёт `stock`, `reserve`, `quantity`), но
  раз резерва больше нет, `stock` в результате практически всегда равен
  физическому остатку — оставлено как есть, задел на будущее если резерв
  понадобится снова.

**Инцидент 2026-08-21:** заказ `67882` был вручную переведён в `cancelled`
из карточки, но товар не вернулся в МойСклад. В логах было только обновление
заказа без `Order cancelled` и без распроведения demand. Demand заказа
распроведён вручную идемпотентным `DemandService::returnOrderStock()`, остаток
возвращён. Путь inline-обновления исправлен, чтобы последующие ручные отмены
автоматически ставили возврат товара в очередь.

### 4. Крон-автоотмена неоплаченных заказов (2 часа)

- **`App\Console\Commands\CancelUnpaidOrdersCommand`**
  (`orders:cancel-unpaid`) — находит заказы `status IN (new, processing)`
  и `payment_status != paid`, созданные более 2 часов назад, отменяет через
  существующий `cancelOrder()` (который уже обновляет статус в МойСклад и
  запускает возврат товара).
- Зарегистрировано в `routes/console.php`:
  `Schedule::command('orders:cancel-unpaid')->everyFiveMinutes()->withoutOverlapping()->runInBackground()`.

**Подтверждено тестом:** заказ с искусственно состаренной `created_at`
(-3 часа) был найден командой, отменён, товар возвращён на склад
(`stock` восстановлен полностью). Дополнительно подтверждено на
реальном заказе в статусе `processing` — крон корректно отменил его
после расширения критерия.

**Инцидент при тестировании (устранён):** расширение критерия крона на
`processing` первым запуском массово отменило ~20 давних тестовых
заказов в БД (изначально принятых за легаси-продакшен-данные — crон
успел отработать повторно по расписанию до завершения проверки).
Заказы подтверждены как тестовые, отмена для них корректна, откат не
требовался.

### 4.1 Возврат товара на склад при статусе «Возврат товара»

При переходе заказа в `PRODUCT_RETURN` (вручную менеджером через
`OrderController::updateStatus()`, или автоматически от
Yandex/CDEK Observer'ов) `OrderCreationService::updateOrderStatus()`
дополнительно ставит в очередь `ReturnOrderStockToMoySkladJob`, если у
заказа есть `moysklad_demand_uuid` — товар физически возвращается на
склад (демонд распроводится). Реализовано централизованно (один путь
для ручной и автоматической смены статуса), решение открытого вопроса
№4 — выбран вариант «распровести существующий `demand`», без создания
отдельного документа `salesreturn`.

**Подтверждено тестом:** заказ со списанным товаром (`demand`
создан и проведён, `stock 100 → 96`) при переходе в `PRODUCT_RETURN`
вернул товар на склад (`stock 96 → 100`), лог подтверждает
«товар возвращён на склад (demand распроведён)».

### 5. Кнопка «Отменить оплату» + эндпоинт возврата (решение открытого вопроса №1)

Решение: **два разных эндпоинта под разную авторизацию**, а не один
смешанный.

- **`POST /api/orders/{order}/refund-payment`** (admin, `auth:sanctum`) —
  `OrderController::refundPayment()`. Вызывается кнопкой из дашборда.
  Проверяет права (`OrderAuthorizationService::canUpdate()`), что заказ
  оплачен (`Order::isPaid()`), находит последний `Payment` со статусом
  `completed`, вызывает `app('payment')->refundPayment($payment)`
  (существующий `CloudPaymentProvider::refundPayment()` — POST
  `/payments/refund` в API CloudPayments). При успехе — `Order::status`
  → `RETURN_PAYMENT`.
- **`POST /api/webhooks/cloudpayments/refund`** (публичный, проверка
  подписи `Content-HMAC`, как `check`/`pay`/`fail`) —
  `CloudPaymentsController::refund()`. Слушает асинхронное подтверждение
  возврата от CloudPayments (по документации CloudPayments: возврат по
  СБП всегда асинхронный, по картам может быть отложенным — финальный
  статус приходит именно через это уведомление). Ищет платёж по
  `PaymentTransactionId` (номер **оригинальной** транзакции оплаты в
  CloudPayments) — в Refund-уведомлении метаданные/`ExternalId` не
  гарантированы, в отличие от `Pay`/`Fail`. Идемпотентно (проверка
  `! $payment->isRefunded()`).
- **Кнопка в дашборде** (`again_dashboard/src/components/orders/view/
  partials/OrderStatuses.vue`) — видна только при `payment_status = paid`
  и `status != return_payment`. `window.confirm()` перед вызовом (действие
  необратимо). При успехе — `emit('refresh')`, родитель (`OrderView.vue`)
  перезагружает заказ.

**Подтверждено тестом:** реальный вызов `OrderController::refundPayment()`
с фейковым `provider_payment_id` корректно дошёл до API CloudPayments,
получил ожидаемый отказ, вернул `500` с понятным сообщением, статус
заказа не изменился (правильное поведение — статус меняется только при
успешном возврате). Полный успешный сценарий (реальный `TransactionId` от
настоящего платежа) не тестировался — требует реального оплаченного
заказа в CloudPayments для чистого теста.

**Побочная находка при реализации:** `PaymentService` зарегистрирован в
контейнере под строковым алиасом `'payment'`
(`AppServiceProvider::register()`), а не под именем класса —
`app(PaymentService::class)` не резолвится (конструктор требует
`array $config`, DI контейнер не может его вывести). Исправлено на
`app('payment')`.

---

## Побочная находка (не входит в скоуп задачи, требует отдельного решения)

При тестировании `cancelOrder()` выявлено: поля `cancellation_reason` и
`cancelled_at`, которые метод пытается записать через `$order->update([...])`,
**не существуют как колонки в таблице `orders`** — Eloquent их тихо
игнорирует (Mass Assignment не бросает ошибку на отсутствующий в `$fillable`/
несуществующий столбец в некоторых конфигурациях). Причина/возраст баге не
исследован. Не мешает работе МойСклад-синхронизации (статус `cancelled`
сохраняется корректно), но причина отмены сейчас нигде не фиксируется в БД.
Нужно отдельно решить: добавить колонки миграцией, или это уже где-то
пишется иначе (например, через `OrderHistoryService`).

---

## Описание (исходное, из ТЗ)

**Сценарий:** админка → заказы → все заказы → карточка заказа.

Актуализировать список статусов заказа и логику их переключения, синхронизировав
это с интеграцией МойСклад (резервирование/списание остатков) и вебхуком
CloudPayments (возврат оплаты).

### Факт (было на момент постановки задачи)

Текущий список статусов (`App\Enums\OrderStatus`):

```
Новый, В работе, Отгружен, Отгружен на экспорт, Доставлен, Отменен, Возврат товара
```

Логика переключения по большей части ручная (менеджер сам меняет статус в
интерфейсе), кроме:
- Смена `payment_status` на `paid` при вебхуке CloudPayments `pay` → диспатчит
  `OrderPaid`, который ставит в очередь создание доставки (Яндекс/СДЭК) и
  синхронизацию с МойСклад (см. `SyncOrderToMoySkladAfterPayment`).
- `cancelOrder()` меняет статус на `cancelled` и (если заказ уже выгружен в
  МойСклад) обновляет `state` документа там же на статус «Отменен».

### План-результат (исходный)

**1. Список статусов** (`App\Enums\OrderStatus`):

```
Новый → В работе → Отгружен → Доставлен → Отменен → Возврат товара → Возврат оплаты
```

- Убрать: `Отгружен на экспорт` (`SHIPPED_EXPORT`).
- Добавить в конец: `Возврат оплаты` (новый case, отдельно от существующего
  `PaymentStatus::REFUNDED`, который относится к статусу *платежа*, не заказа).

**2. Логика по каждому статусу:**

| Статус | Кто/что переключает | Описание |
|---|---|---|
| Новый | Автоматически при создании заказа | Как сейчас |
| В работе | Менеджер вручную в админке | Как сейчас |
| Отгружен | Автоматически — когда служба доставки (ЯД/СДЭК) забрала заказ и оформила у себя на складе/в своей системе | Заказ считается физически покинувшим склад |
| Доставлен | Автоматически — когда доставлено в ПВЗ или на адрес клиенту | Финальный успешный статус |
| Отменен | Автоматически — если после оформления заказа в течение 2 часов не произошла оплата | Требует: возврат/списание товара в МойСклад + смена статуса заказа |
| Возврат товара | Автоматически — клиент не забрал заказ из ПВЗ / не забрал товар дома, заказ вернулся отправителю (по данным ЯД/СДЭК) | |
| Возврат оплаты | Менеджер вручную через кнопку в карточке заказа | Клиент отказался от покупки после оплаты — ручной возврат денег через CloudPayments |

**5. Кнопка «Отменить оплату» (Возврат оплаты):**

- Новая кнопка в карточке заказа в админке (`again_dashboard`), видна при
  `payment_status = paid` (или соответствующих статусах заказа).
- По клику — вызов `POST /api/webhooks/cloudpayments/refund`.
- Существующий метод `CloudPaymentProvider::refundPayment()` уже умеет
  делать возврат через API CloudPayments (`POST /payments/refund`) и
  переводить `Payment::status` в `REFUNDED` — не хватает только: (а)
  admin-эндпоинта, который его вызывает, (б) обновления `Order::status` на
  новый статус «Возврат оплаты» после успешного возврата.

---

## Открытые вопросы (требуют уточнения перед реализацией пункта 2)

1. ~~**Эндпоинт `/api/webhooks/cloudpayments/refund`**~~ — **Решено**, см.
   раздел «5. Кнопка «Отменить оплату»» выше: два раздельных эндпоинта
   (admin `POST /orders/{id}/refund-payment` для ручного действия
   менеджера + публичный webhook `POST /webhooks/cloudpayments/refund`
   для асинхронного подтверждения от CloudPayments).

2. **Порядок статусов при автоотмене** — **Решено**: `orders:cancel-unpaid`
   теперь проверяет заказы и в статусе `new`, и в статусе `processing`
   («В работе»). Подтверждено тестом на реальных данных.

3. ~~**«Возврат товара»** — как технически определяется момент возврата~~
   — **Решено**: автоматически, через `internal_status` в
   `YandexOrder`/`CdekOrder` (уже обновляется существующими Poll-джобами,
   Observer'ы подписаны на изменение этого поля). См. раздел
   «2. Автоматизация статусов…» выше.

4. **Демонд (`demand`) при возврате товара** — **Решено**: выбран вариант
   (а) — распроведение существующего `demand` (`applicable: false`), без
   создания отдельного документа `salesreturn`. Подключено централизованно
   в `OrderCreationService::updateOrderStatus()` — срабатывает и при
   ручной смене статуса менеджером, и автоматически от Yandex/CDEK
   Observer'ов. Подтверждено тестом: `stock 96 → 100` при переходе в
   `PRODUCT_RETURN`.

5. **Обратная совместимость данных:** уже есть заказы со статусом
   `shipped_export`, если такие есть в БД — что с ними делать при удалении
   этого case из enum (перевести в `shipped`? оставить как legacy-значение
   без case в enum?). **Нужно выполнить перед удалением case из enum:**
   `SELECT COUNT(*) FROM orders WHERE status = 'shipped_export'` — если > 0,
   решить миграцию данных.

---

## Технические точки (актуализировано)

| Что | Где |
|---|---|
| Список статусов заказа | `app/Enums/OrderStatus.php` — **обновлён**: `SHIPPED_EXPORT` убран, `RETURN_PAYMENT` добавлен |
| Статус оплаты (отдельный enum) | `app/Enums/PaymentStatus.php` (уже есть `REFUNDED`) |
| Смена статуса заказа | `OrderCreationService::updateOrderStatus()`, `cancelOrder()` |
| Синхронизация заказа с МойСклад (документ) | `App\Services\MoySklad\OrderService::pushOrder()` — создаёт/обновляет `customerorder`, сопоставляет статус со `state` по названию (`resolveStateMeta()`). Резерв на позициях убран |
| Физическое списание/возврат товара | `App\Services\MoySklad\DemandService` (`shipOrder()`, `returnOrderStock()`) — **новое** |
| Постановка синхронизации в очередь | `App\Jobs\SyncOrderToMoySkladJob` (создаёт/обновляет заказ + списывает через `DemandService`), `App\Jobs\ReturnOrderStockToMoySkladJob` (возврат товара) — **новое** |
| Крон автоотмены | `App\Console\Commands\CancelUnpaidOrdersCommand` (`orders:cancel-unpaid`), зарегистрирован в `routes/console.php` — **новое** |
| CloudPayments вебхуки (паттерн) | `App\Http\Controllers\Api\Public\Order\CloudPaymentsController` (`check`, `pay`, `fail`) — по этому паттерну добавлять `refund`. **Не реализовано** |
| Возврат платежа через API CloudPayments | `App\Services\Payment\CloudPaymentProvider::refundPayment()` — уже реализован, не хватает вызывающего эндпоинта |
| Модель `Payment` | `app/Models/Payment.php` — `STATUS_REFUNDED` уже есть |
| pm2-процесс для scheduler/cron | `laravel-scheduler` (уже работает в проде) |
| Интеграция с доставкой (для статуса «Отгружен»/«Доставлен»/«Возврат товара») | `App\Services\Delivery\YandexDeliveryService`, `App\Services\Delivery\CdekDeliveryService`, `App\Jobs\PollYandexDeliveryStatusesJob`, `App\Jobs\PollCdekDeliveryStatusesJob` (не менялись) + **новое**: `App\Observers\YandexOrderObserver`, `App\Observers\CdekOrderObserver` (автоматически переключают `Order::status`) |

### МойСклад — подтверждённые фактами (тестами) детали API

- Резерв позиции заказа: `PUT /entity/customerorder/{id}/positions/{positionId}`
  с телом `{"reserve": N}` — не влияет на физический остаток (`stock`),
  влияет на «доступное количество» (`quantity` в отчёте
  `/report/stock/all`, где `quantity = stock - reserve`). **Больше не
  используется в текущей реализации** (решение изменено на прямое списание).
- Отчёт `/report/stock/all/current` **не отдаёт** `reserve` — только
  физический `stock`. Отчёт `/report/stock/all` отдаёт оба поля плюс
  посчитанное `quantity` (доступно). Используется в
  `MoySkladHelperService::check_stock()`.
- Смена `state` документа `customerorder` на статус со `stateType:
  Unsuccessful` (например, «Отменен») **автоматически снимает резерв**, если
  он был. С текущей реализацией (без резерва) это не имеет значения для
  остатков — товар возвращается через отдельный вызов `returnOrderStock()`.
- Смена `state` на «Отгружен» (`stateType: Regular` в этом аккаунте) **тоже
  снимает резерв**, но **не списывает** физический остаток сама по себе —
  требуется отдельный документ `demand`.
- Физическое списание: `POST /entity/demand` с обязательными
  `organization`, `agent`, `store` (получить через `GET /entity/store`),
  опционально `customerOrder` (привязка к заказу), `positions`.
  `demand.applicable: true` — списывает остаток, `false` — распроводит
  (возвращает остаток обратно, документ не удаляется). **Реализовано** в
  `DemandService`.
